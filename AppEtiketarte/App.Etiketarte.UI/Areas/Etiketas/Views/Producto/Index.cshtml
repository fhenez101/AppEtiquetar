@model IEnumerable<App.Etiketarte.Model.Producto>

<h2>Productos</h2>

<section class="main-container col-md-9">
    <div>
        <div class="control">
            @Html.AddAction("Create")
        </div>
        <table class="table table-bordered table-responsive jtv-cart-summary">
            <thead>
                <tr>
                    <td>@Html.DisplayNameFor(model => model.Nombre)</td>
                    <td>@Html.DisplayNameFor(model => model.Codigo)</td>
                    <td class="fila-centrada">@Html.DisplayNameFor(model => model.Estado)</td>
                    <td class="fila-centrada">Acciones</td>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Nombre)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Codigo)
                        </td>
                        <td class="fila-centrada">
                            @Html.DisplayFor(modelItem => item.Estado)
                        </td>
                        <td class="fila-centrada">
                            @Html.EditAction("Edit", "Producto", new { id = Html.Slug(item.IdProducto, item.Nombre) })
                            @Html.DetailAction("Details", "Producto", new { id = Html.Slug(item.IdProducto, item.Nombre) })
                            @Html.DeleteAction("Delete", "Producto", new { id = Html.Slug(item.IdProducto, item.Nombre) })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</section>